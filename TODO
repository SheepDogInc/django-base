TODO:

- (probably always) bump the Django version (and any other requirements that are out of date)
- use the new settings structure (settings module, with base, local, production, etc)
- add some more fabfile commands for common tasks
